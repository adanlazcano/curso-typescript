(() => {
  enum AudioLevel {
    min = 1,
    medium,
    max = 9,
  }

  let currentAudio: AudioLevel = 14;

  console.log(AudioLevel);
  // console.log(AudioLevel.medium)
    // console.log(AudioLevel.max);
    // console.log(currentAudio);
})();
